services:
  - type: web
    name: tracelab
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: waitress-serve --host=0.0.0.0 --port=$PORT backend.server:app
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
